# 本地验证记录

## 0.6.0 — 2026-09-21

本地 Windows / Python 3.12 执行 `python -X utf8 -B -m unittest discover -s tests -v`：**236 项通过，0 跳过，62.936 秒**。覆盖详情组与中断恢复、城市控件交互、菜单切换和滚动诊断，以及已有运行时与离线 Chromium DOM 回归；本轮没有执行招聘平台外发。

TypeSafe 官方 Skill 已通过 Codex 安装并校验。使用本机用户环境中的 `TYPESAFE_API_KEY` 向官方端点发送一条虚构 Python 后端岗位，实际返回模型 `jev-1.13.0`、Choice `python_backend`，请求成功；密钥未输出或写入文件。这只验证鉴权、连通性和响应契约，不代表真实岗位判断准确率已校准。Windows 已启动宿主未继承新环境变量的读取方式已补入 Skill。

Skill 结构与 diff 校验通过；结构校验器首次按 Windows 默认 GBK 解码失败，显式使用 `-X utf8` 后通过，未修改校验器。发布包使用仓库打包器并逐文件核验 SHA-256。远端 CI 状态以发布提交的 GitHub Actions 为准。

## 0.5.0 — 2026-09-20

本地执行 `python -B -X utf8 -m unittest discover -s tests -v`：**214 项通过，0 跳过，47.799 秒**（Windows / Python 3.12）。测试覆盖状态机、模拟传输和断网 Chromium DOM；不把离线发送样例当作招聘平台发送回执。

真实 Kimi 只读性能对照：同一可见会话页、两个既有会话交替各 4 个样本，均以身份和消息稳定为终点。旧单步流程平均 1378.15 ms、4 次浏览调用、3 次入口调用；业务闭环平均 1112.40 ms、3 次浏览调用、1 次入口调用。执行器平均耗时减少约 19.3%，浏览调用减少 25%。两组均 4/4 ready、0 恢复；发送 unknown 比例不适用于此只读对照。没有测量模型思考及宿主工具往返，不宣称整体任务快同样比例。

首次闭环实现的 4 次耗时为 2482.02 / 1362.34 / 1394.97 / 1411.14 ms，并未改善整体均值。原因之一是每个等待样本都写入完整状态；改为完整保存原始样本、最终更新一次状态后得到上面的结果。瞬时访问限制仍优先保留。原始失败与慢样本存于本地证据，不删除或改成成功。

- 离线验证：普通回复一次业务入口、5 次浏览调用；超时后 unknown，后续同 action ID 只核对；提交传输含糊只被动恢复；新来信、错收件人、授权不足在相应边界拒绝。测量落盘失败不会把已成功发送变成可重试动作。
- 已有授权线上维护验证：一条普通回复有新送达回执；简历分享停留在请求已发送/等待对方同意，未声称附件真正送达。新 reply-and-verify 业务入口发送路径仅离线验证，没有为了测速重复外发。
- 真实只读验证：招聘方系统附件请求的新文案可识别；其具体“同意”点击仍未线上验收。旧批次、保留候选和未完成 backlog 在切页恢复后保留。焦点模拟曾使自然列表新增 4 个 ID，不能据此保证长期加载。
- 首次测试曾出现 mock 页面缺少控件导致 loading 分类、异步 DOM harness 未等待 Promise 等测试问题；修正样例与 harness 后重跑。另有 Kimi 同站 find_tab 返回错误路径，保留原失败并改用正常导航，不更换通道。
- 线上 ensure-page 首次复用聊天页时误用了搜索页观察器，得到 loading/pending；改为按目标页选择观察器并新增回归后，真实复用结果 ready，2 次只读浏览调用，716.512 ms。原始 pending 结果保留。
- 公共发布包只包含源码、脱敏测试和说明；个人策略、履历、聊天、运行 token 与原始证据留在本地。未验证其他站点、长期无人值守或 150 次持续外发。

## 0.4.1 标签恢复修复 — 2026-09-16

173项本地测试通过（0跳过，40.797秒）。新增同session标签重建、已有页复用、访问限制、未知导航防重复、选择途中标签关闭、底层读取错误保留和加载分类回归。第一次测试发现旧用例把裸“请稍候”当安全检查，与修复目标冲突；保留首次失败输出，修正该旧断言后全套通过。

真实Kimi验证：原任务session标签已关闭，通过recover-page在同session打开BOSS职位页，随后inspect读到正常页面、已登录账号及15张卡片。恢复页面默认筛选与当日计划不同，需要按当前policy重新设置；本次恢复没有发送或重置投递历史。这验证的是标签恢复，不是剩余投递完成。

## 0.4.1 — 2026-09-16

本地 Windows / Python 3.12 执行 `python -B -X utf8 -m unittest discover -s tests -v`：**164 项通过，0 跳过，40.422 秒**。测试使用临时目录、模拟传输及离线 Chromium，没有发送真实招聘消息。

- 新增验证：恢复命令无 token、无浏览器连接也能读回原查询/session及策略指纹；冲突 provider 或 fallback 配置在任何传输前拒绝；旧配置未声明浏览器时仍固定 Kimi。
- 包含查询词与完整平台选项约束、列表尾部剩余滚动空间、查询结束状态重置等回归验证。
- Plugin/Skill 结构校验、相对链接与 diff 检查通过。Windows Skill 校验首次受系统 GBK 解码影响失败，使用 `-X utf8` 后通过；未修改校验器。
- 求职网页固定 Kimi 是插件指令与正式入口约束，不是宿主沙箱；不能拦截 Agent 绕开插件直接调用其他工具。真实压缩后续跑仍应根据实际工具日志验收。
- 本版不包含另外的实验性聊天发送适配；普通回复/附件的网页执行保持 unsupported。保留既有个人状态与未核对动作，不以升级重发。
- GitHub Actions 检查发布提交，发布包逐文件验证 SHA-256；远端实际状态以该提交的 Actions 为准。

## 0.4.0 — 2026-09-15

本版区分状态机测试、离线 Chromium 页面行为和真实网站结果。

发布前本地执行 `python -B -X utf8 -m unittest discover -s tests -v`：**147 项通过，0 跳过**（Windows / Python 3.12，34.915 秒）；Plugin 与 Skill 校验通过，65 条相对文档链接有效。发布包构建会逐文件复核内置 SHA-256 清单。

- 核心测试使用临时目录、合成策略和模拟传输；覆盖列表与详情顺序、异常暂停、账号限制归属、资料变更后重审及未知外发恢复。
- 页面测试使用断网 Chromium 渲染合成 DOM，验证可见筛选控件、详情身份、单次点击、单次滚动和回执解析；不是招聘网站端到端测试。
- CI 为 Windows / Linux × Python 3.10 / 3.12，安装固定 Playwright 与 Chromium，缺少依赖时失败。发布提交的实际状态查看 GitHub Actions，不以本地结果代替。
- 发布前已有一次独立授权的新账号实测：平台筛选、当前列表、一个详情、一次默认招呼；即时回执未出现时记 unknown，后续被动观察到匹配的成功提示后记 succeeded，没有重发。原始记录留在本地，公共仓库不包含个人账号、岗位链接或聊天截图。
- 未验证：50/150 次持续投递、普通回复、附件、其他站点及无人值守登录。一次成功不能证明以后不触发限制。
- 核心状态保持 v2；现有运行应结束后再升级，保留个人数据和未核对动作。

## 0.3.0 — 历史验证

日期：2026-09-08。环境：Windows，Python 3.12.7。功能测试使用临时目录中的合成资料，没有访问真实招聘账号或短信。

- `python -B -m unittest discover -s tests -v`：48项通过。包括原有26项及22项新增测试。
- Codex plugin-creator 的 validate_plugin.py：通过。
- skill-creator 的 quick_validate.py：通过。
- doctor.py 无参数只读诊断：Python与Skill结构可用，浏览器账号正确标为未验证。
- 打包测试验证同一输入生成相同文件摘要、排除个人数据、拒绝覆盖已有输出；实际压缩包逐文件校验。
- 功能提交 `0f305a4` 的 [GitHub Actions](https://github.com/ShiqinGuo/job-hunter/actions/runs/34200703713)：Windows / Linux × Python 3.10 / 3.12 四组全部通过。
- 使用独立 Codex 配置目录，从 `ShiqinGuo/job-hunter` 添加 marketplace 并安装成功；本机 Codex 和 Claude Code 安装副本的 Skill 内容均与源码一致。

重点行为：草稿策略拒绝外发；已有一次性授权不改变长期模式；账号与平台范围匹配；公司别名、猎头、招聘项目排除；授权附件hash；配置并发变更与提交前重查；邀面与确认分开；错过回复时段合并；漏跑日报发现；日报生成与交付分开；中断恢复保留未知动作；传输超时不重试提交。

未验证：新版真实浏览器提交、各招聘网站语义兼容、各宿主通知回执、无人值守登录。既有现场经验保留为平台参考，不计入这版代码的端到端验收。

安装时先确认运行结束，再按 runtime.md 迁移新动作参数，在新对话验收。个人简历、策略和申请历史不包含在本仓库或发布包中。
