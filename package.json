{
  "name": "job-hunter-dev",
  "private": true,
  "version": "0.4.1",
  "description": "Offline DOM test dependencies; not required for plugin runtime.",
  "devDependencies": {
    "playwright": "1.62.1"
  }
}
