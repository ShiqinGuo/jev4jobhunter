# 维护方向

## 0.4.0 已实现

- 单步浏览状态检查、当前批次与单个详情约束、异常持久化。
- 账号上下文、资料与策略变化后重新审核，保留既有外发防重。
- BOSS 当前页面结构适配与平台默认招呼回执；一次真实新沟通验证。
- 按需信息引导及招聘反馈复盘规则。
- 离线 Chromium 页面测试，CI 安装固定开发依赖并拒绝静默跳过。

## 0.3.0 已实现

- 策略集中在 policy.json，带指纹比较后更新和备份。
- 登记与提交前校验授权、账号、公司 / 猎头 / 项目排除、附件版本。
- 日报生成与交付分离，提供补报、回复时段合并、旧轮次恢复。
- 只读安装诊断与可选 Kimi UTF-8 传输辅助库。
- 隔离测试、文件校验清单及 Windows / Linux CI 配置。

## 验证边界与后续工作

0.4.0 已有一次 Kimi WebBridge 新沟通回执，另有隔离状态机和离线 DOM 测试；未验证持续批量运行、普通回复、附件、登录恢复或所有浏览器。通道沿用用户选择和站点实测，不把某台电脑的失败推广到所有用户。

- 将普通回复与附件发送接入统一入口，并分别做独立授权的真实回执验收。
- 增加完整脱敏页面样例，覆盖滚动加载、错位详情、简历请求和人工接管；传输测试不代替这些语义验证。
- 改善稳定职位 / 会话 ID 与别名关联，减少低可信临时键。
- 补充各宿主实际通知回执；未知交付先核对，不盲目重复推送。

不通过私有接口、伪造验证结果或绕过平台额度提高投递量。
