# 变更记录

## 0.5.0 — 2026-09-20

- 适配已有 BOSS 会话回复、原生简历分享、确认与回执核对；身份、最新来信、手动接管、材料版本和历史动作都在提交前检查。
- 增加打开会话/详情后条件等待、确保当前页面、回复并核验及只读等待回执的业务入口。模型先审阅，执行器收拢机械步骤；unknown 不重发。
- 按业务操作保存耗时、浏览调用、等待、恢复、结果及原始证据；CLI 默认紧凑返回，full 可展开。按文件内容缓存稳定策略校验，不缓存动态提交条件。
- 修复同站标签选择错页、详情骨架屏、已保留候选复审和搜索断点恢复；按 session 登记完整页面 URL，临时焦点模拟可显式撤销。
- 区分简历请求已发送、等待对方同意与附件真正送达；兼容招聘方系统附件请求文案，不将电话或地点同意当作简历授权。

状态格式保持 v2，保留原 pending/unknown 与候选历史；不需要重新初始化。线上及离线验证边界见 VALIDATION.md。

## 0.4.1 — 2026-09-16

- 网页通道固定为 Kimi WebBridge；消除宿主浏览器优先与备用通道规则，覆盖截图、观察及恢复。
- 新增只读 resume-context：压缩、新轮次和中断后读取通道、策略指纹、session、账号限制与断点；实际执行前拒绝冲突浏览器配置。
- 查询词受 search.queries 约束，固定经验与薪资选项必须完整匹配 selectedLabels。
- 卡片末尾仍有滚动空间时继续当前查询；暂时无新 ID 不算耗尽，新查询不继承旧 endOfList。
- 统一自主执行、澄清和完成条件；用户允许的经验范围与真实履历分开处理，避免重复确认和提前收尾。

边界：普通聊天回复、附件与其它站点仍未纳入正式网页适配；不通过换浏览器或运行中自改适配绕过。状态保持 v2，旧成功和未知动作继续去重。

## 0.4.0 — 2026-09-15

- 新增统一单步浏览入口：平台筛选读回、当前列表粗筛、单个详情判断、外发回执和单次滚动按状态推进。
- 新增浏览限制持久化与账号上下文；保留原限制，根据明确归属和当前页面证据判断适用范围。
- 资料或策略指纹变化后，旧候选审核失效；历史成功、pending、unknown 继续防重。
- 补齐 BOSS 可见显示名、搜索链接、城市/经验/学历筛选器、卡片城市与平台默认招呼的页面适配。
- 默认招呼发送前未知正文保持未知；发送后记录实际正文，迟到回执通过被动核对恢复，不重复点击。
- 新增信息补充引导与招聘反馈复盘规则，明确正式工龄、实习、软硬条件和面试方式。
- 单步新沟通入口仅接受已授权的平台默认招呼，自定义文案在任何浏览器调用前拒绝，避免发送内容与请求不一致。
- CI 安装固定 Playwright/Chromium，缺依赖时失败，避免跳过整组页面测试。
- README 改为功能、流程、快速开始和支持范围；发布验证区分离线浏览器测试与单次真实沟通。

兼容与边界：状态格式保持 v2；本版正式网页入口仅适配 BOSS 搜索、单个详情与新沟通。普通聊天回复、附件及其他站点尚未接入，保存材料并报告缺口，不使用旧裸调用兜底。升级在当前运行结束后进行，保留个人目录，在新对话加载。

## 0.3.0 — 2026-09-08

- 结构化长期与一次性授权校验，draft/ask 不再仅凭说明文本登记外发。
- 公司别名、猎头发布者、招聘项目排除；旧会话发简历也重查。
- check-action 防止登记后配置改变或附件被替换仍继续提交。
- effective-config / set-policy 提供唯一策略、备份及并发覆盖检查。
- run_ledger 提供轮次去重、恢复、漏报补齐，日报生成与交付分别记录。
- 面试阶段需对应证据；本地报告支持按日期过滤。
- 只读 doctor、可选 Kimi 传输库、行为测试、CI、发布包校验清单。

- MIT 许可证、Codex / Claude Code marketplace 入口和可校验的发布包。

兼容：状态仍为 v2；新动作按运行指南补充账号、一次性授权和目标事实。旧 pending/unknown 先核对回执。升级应在运行锁释放后切换源码，保留个人数据。
